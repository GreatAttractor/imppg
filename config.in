#
# ImPPG
#
# User-configurable build variables
#

# To disable CFITSIO (FITS format support), change 1 to 0 below:
set(USE_CFITSIO 1)

# To disable FreeImage (multiple image formats support), change 1 to 0 below:
set(USE_FREEIMAGE 1)

# To disable OpenGL back end (processing on the GPU), change 1 to 0 below:
set(USE_OPENGL_BACKEND 1)

# When building under MS Windows, the following paths have to be set appropriately:
set(WXWIDGETS_ROOT_DIR C:/Users/Filip/Downloads/temp/wxWidgets-3.1.3)
set(WXWIDGETS_BUILD_DIR C:/Users/Filip/Downloads/temp/wxWidgets-3.1.3/build_msys2_rel)
